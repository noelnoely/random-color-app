# Random Color App

A simple Flutter application that displays `Hello there` in the center of the screen and changes the background color after tapping anywhere on the screen.

## Features

- Displays `Hello there` in the center
- Changes the background color on tap
- Generates random RGB colors
- Adjusts text color for better contrast

## Run

```bash
flutter pub get
flutter run
